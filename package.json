{
  "name": "college-resource-dashboard",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix client",
    "dev:client": "npm start --prefix client",
    "dev:server": "npm run dev --prefix server",
    "setup:server": "npm run setup --prefix server"
  }
}
